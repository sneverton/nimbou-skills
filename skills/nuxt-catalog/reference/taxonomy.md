# Taxonomy

Allowed `category` values:

- `app`
- `display`
- `feedback`
- `form`
- `navigation`
- `overlay`
- `section`
- `domain`

`domain` must be `null` for base or global components and a non-empty string for business-specific components.
